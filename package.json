{
  "name": "hhaus-org-github-io",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Astro marketing site for the global H/HAUS hacker-house network.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "test": "npm run build && node --test tests/*.test.mjs",
    "verify": "npm run check && npm test"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "astro": "7.2.9"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "6.0.3"
  }
}
